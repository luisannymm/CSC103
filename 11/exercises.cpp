/* TODO: try to write the polynomial evaluation function we started
 * in lecture. */
/* TODO: actually write down (and test) push_front and pop_front
 * (we outlined them in class, but it may still be instructive to
 * actually write it down yourself). */
